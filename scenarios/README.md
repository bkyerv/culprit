# Scenarios

Each scenario is data: a `scenario.yaml` containing the task, workspace seed, criteria, and
capability policy, plus files beneath `workspace/`. The execution engine does not contain
scenario-specific logic.

`supplier-counter-offer/` is the P1 hero scenario. Its synthetic `.example` supplier addresses are
safe test data, and all attempted effects are simulated by the broker.
