[project]
name = "culprit-core"
version = "0.0.0"
description = "Shared Culprit domain primitives"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.10,<3",
  "pyyaml>=6,<7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
